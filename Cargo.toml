[package]
name = "xor-ll"
version = "0.1.0"
edition = "2024"

[dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[dev-dependencies]
stats_alloc = "0.1"
byte-unit = "5.2"

[[bench]]
name = "performance"
harness = false

[[bench]]
name = "memory"
harness = false